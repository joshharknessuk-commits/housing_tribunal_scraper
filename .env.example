# Base listing page to crawl
TRIBUNAL_BASE_URL='https://www.gov.uk/ftt-housing-and-property/decisions'

# Optional: directory where downloaded PDFs should be stored
OUTPUT_DIR='./outputs'

# Optional: Neon/Postgres connection for inserting scraped metadata directly
NEON_URL='postgresql://username:password@host/dbname?sslmode=require&channel_binding=require'

# Optional batching controls
MAX_PAGES=5
REQUEST_TIMEOUT=30
REQUEST_RETRY_TOTAL=3
REQUEST_RETRY_BACKOFF=0.5
HTTP_USER_AGENT='Housing-Tribunal-Scraper/1.0'
